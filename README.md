# hanzi-origin

Small Python package skeleton for Hanzi origin utilities.

Quick start:

- Create a virtualenv and install requirements:
  - `python -m venv .venv && .venv\\Scripts\\activate`
  - `python -m pip install -r requirements.txt`
- Run tests:
  - `pytest`

Layout: